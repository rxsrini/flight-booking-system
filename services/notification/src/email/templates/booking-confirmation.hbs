<!DOCTYPE html>
<html>
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Booking Confirmation</title>
  <style>
    body { font-family: Arial, sans-serif; line-height: 1.6; color: #333; max-width: 600px; margin: 0 auto; padding: 20px; }
    .header { background: linear-gradient(135deg, #667eea 0%, #764ba2 100%); color: white; padding: 30px; text-align: center; border-radius: 10px 10px 0 0; }
    .content { background: #f9f9f9; padding: 30px; border-radius: 0 0 10px 10px; }
    .booking-details { background: white; padding: 20px; border-radius: 8px; margin: 20px 0; border-left: 4px solid #667eea; }
    .detail-row { display: flex; justify-content: space-between; padding: 10px 0; border-bottom: 1px solid #eee; }
    .detail-label { font-weight: bold; color: #666; }
    .detail-value { color: #333; }
    .pnr { font-size: 28px; font-weight: bold; color: #667eea; margin: 20px 0; text-align: center; letter-spacing: 2px; }
    .footer { text-align: center; margin-top: 30px; color: #666; font-size: 12px; }
    .btn { display: inline-block; background: #667eea; color: white; padding: 12px 30px; text-decoration: none; border-radius: 5px; margin-top: 20px; }
  </style>
</head>
<body>
  <div class="header">
    <h1>✈️ Booking Confirmed!</h1>
    <p>Your flight has been successfully booked</p>
  </div>

  <div class="content">
    <p>Dear {{customerName}},</p>

    <p>Thank you for booking with us! Your flight reservation has been confirmed.</p>

    <div class="pnr">{{pnr}}</div>
    <p style="text-align: center; color: #666;">Your booking reference number</p>

    <div class="booking-details">
      <h3 style="margin-top: 0; color: #667eea;">Flight Details</h3>

      <div class="detail-row">
        <span class="detail-label">Flight Number:</span>
        <span class="detail-value">{{flightNumber}}</span>
      </div>

      <div class="detail-row">
        <span class="detail-label">Airline:</span>
        <span class="detail-value">{{airline}}</span>
      </div>

      <div class="detail-row">
        <span class="detail-label">Route:</span>
        <span class="detail-value">{{origin}} → {{destination}}</span>
      </div>

      <div class="detail-row">
        <span class="detail-label">Departure:</span>
        <span class="detail-value">{{departureDate}} at {{departureTime}}</span>
      </div>

      <div class="detail-row">
        <span class="detail-label">Arrival:</span>
        <span class="detail-value">{{arrivalDate}} at {{arrivalTime}}</span>
      </div>

      <div class="detail-row">
        <span class="detail-label">Passengers:</span>
        <span class="detail-value">{{passengers}}</span>
      </div>

      <div class="detail-row">
        <span class="detail-label">Cabin Class:</span>
        <span class="detail-value">{{cabinClass}}</span>
      </div>

      <div class="detail-row" style="border-bottom: none; margin-top: 10px; padding-top: 15px; border-top: 2px solid #667eea;">
        <span class="detail-label" style="font-size: 18px;">Total Amount:</span>
        <span class="detail-value" style="font-size: 18px; font-weight: bold; color: #667eea;">{{currency}} {{totalAmount}}</span>
      </div>
    </div>

    <p><strong>Important:</strong> Please arrive at the airport at least 2 hours before departure for domestic flights and 3 hours for international flights.</p>

    <p>Keep this PNR handy for check-in and any future reference.</p>

    <div style="text-align: center;">
      <a href="{{frontendUrl}}/bookings/{{pnr}}" class="btn">View Booking Details</a>
    </div>
  </div>

  <div class="footer">
    <p>This is an automated message. Please do not reply to this email.</p>
    <p>&copy; 2024 Flight Booking System. All rights reserved.</p>
  </div>
</body>
</html>
